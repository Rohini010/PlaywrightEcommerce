{
  "name": "rohinibitsproject",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf allure-results allure-report playwright-report",
    "test": "npx playwright test",
    "test:html": "npx playwright test --reporter=html",
    "test:allure": "npx playwright test --reporter=line,allure-playwright",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "test:session": "npx playwright test tests/addProduct.spec.js --workers=1 && npx playwright test tests/cartVerify.spec.js --workers=1 && npx playwright test tests/checkout.spec.js --workers=1"
  },
  "keywords": [],
  "author": "Rohini Shilimkar",
  "license": "ISC",
  "description": "Playwright Automation Project with AIWrapper + Allure Reporting",
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.1.0",
    "allure-commandline": "^2.29.0",
    "allure-playwright": "^3.3.2"
  },
  "dependencies": {
    "fast-levenshtein": "^3.0.0"
  }
}
